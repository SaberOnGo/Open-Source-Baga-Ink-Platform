# v1.1 下一位 AI 直接执行 Prompt

## 任务

从本目录开始执行 Baga Ink Platform Kindle Reference Port v1.1。不得另建平行 Task Design、Execution Prompt、handoff 或 scratch 任务体系。

当前只推进 K0 + K1；K2–K7 先保留为后续依赖计划。

---

## 1. 必读顺序

开始编码前按顺序阅读：

```text
项目 AGENTS.md
相关 Standards / Design / Kindle Architecture Freeze
00_v1.1_总控_范围边界与执行纪律.md
01_v1.1_KOReader与Kindle实现链路研究基线.md
02_v1.1_当前仓库现状与实现差距清单.md
03_v1.1_实现语言绑定与成熟组件复用裁决.md
04_v1.1_代码目录结构与模块边界.md
13_v1.1_分批实施计划与WriteScope.md
14_v1.1_RED_GREEN测试与验收矩阵.md
15_v1.1_真机验证证据备份与恢复计划.md
16_v1.1_上游依赖锁定LicensePatch与构建资产.md
17_v1.1_实现前裁决_默认方案与禁止临时决策.md
19_v1.1_源码核验后逐项自检表.md
```

然后读取当前 Batch 对应的 `05–12` Milestone 文档。

---

## 2. 从 Batch 0 开始

未指定 Batch 时必须执行 Batch 0：

```text
1. inspect current main tree
2. verify governing docs and paths
3. inspect current executable IKP/signing implementation
4. inspect/pin candidate KOReader/koreader-base/FBInk sources
5. locate kindlehf build-target definitions
6. trace KOReader bootstrap/device/screen/input/lifecycle source path
7. identify direct Baga private-entry insertion point
8. establish dependency/license/patch/build manifest baseline
9. establish tests/evidence locations
10. update 01/02/16/17/19 if facts differ
```

Batch 0 不写正式 K0/K1 功能实现。

---

## 3. 固定实现输入

不得重新进行以下通用选型：

```text
Device Adapter Contract is language-neutral.
Kindle KOReader-facing glue is Lua/LuaJIT first.
Existing native libraries remain native.
Do not add FFI merely for language uniformity.
K1 uses direct Baga private entry by default.
.koplugin is fallback only after concrete direct-entry blocker evidence.
LifeBook/IKP never imports KOReader private API.
```

如果事实推翻其中任一项，停止相关实现，记录 evidence，并先形成新的 Task Package version。

---

## 4. Batch 1 — K0

按 `05` 实现：

```text
machine IDL
schema/loader
Base subsystem definitions
frozen snapshot
deterministic codegen
Mock Adapter
Contract Tests
compatibility/reproducibility CI
```

先写/确认 RED，再实现 GREEN。不要把 Kindle-specific object 写入通用 IDL。

---

## 5. Batch 2–3 — K1

按 `06` 实现：

```text
Batch 2:
pin upstreams
reproducible kindlehf build
manifest / patch inventory

Batch 3:
baga-launch development entry
direct Baga private entry
baga/bootstrap.lua
Baga-owned smoke surface
input smoke
diagnostics
real-device cold start / exit / relaunch
```

没有真实 `kindlehf` 时可以完成 host/build/fixture 工作，但不得伪造真机 Gate；将 device-dependent item 标记为 blocked/pending evidence。

---

## 6. 每个 Batch 的完成格式

完成 Batch 时必须报告：

```text
Batch
write scope actually changed
RED tests and result
implementation summary
GREEN tests and result
real-device evidence if applicable
artifact digests
remaining blockers
next allowed Batch
```

同时更新 `19` 的对应状态。

---

## 7. 禁止事项

```text
Do not create a new task package just to restate v1.1.
Do not re-plan Rust vs Lua vs C without evidence.
Do not make .koplugin the normal path because it is easier.
Do not start full LifeBook before Probe.
Do not implement jailbreak automation before device-side Platform is stable.
Do not expose KOReader private APIs to IKP.
Do not claim Kindle compatibility without the required real-device evidence.
```

---

## 8. 结束条件

当前 v1.1 首轮实现目标在 K0 + K1 Gate 均通过后结束；随后才进入 K2 的正式功能 Batch。
