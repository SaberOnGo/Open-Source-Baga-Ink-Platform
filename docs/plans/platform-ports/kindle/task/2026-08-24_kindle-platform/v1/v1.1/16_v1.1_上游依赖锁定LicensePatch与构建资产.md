# v1.1 上游依赖锁定、License、Patch 与构建资产

**职责**: 规定 Kindle Platform 采用的上游组件怎样锁定、怎样记录本地 patch、怎样关联构建与真机证据。

---

## 1. K1 开工前必须建立的 Dependency Manifest

至少覆盖：

```text
KOReader
koreader-base
FBInk when separately pinned/used
Lua/LuaJIT components when independently versioned
Kindle build/package tooling used by Baga
KPM/sh_integration when进入 K6
```

每条记录包含：

```text
project name
upstream repository
commit/tag
source digest when applicable
license
license/notice obligations
Baga usage role
native target
local patch set
build command/toolchain
last verified device/model/firmware
evidence location
```

---

## 2. KOReader / koreader-base Pin

Baga Kindle Platform 第一阶段使用自己锁定、验证的 component set，不依赖用户自行安装的 KOReader。

每次升级必须比较：

```text
bootstrap entry
Kindle Device implementation
screen/input APIs
lifecycle behavior
Reader/UI private APIs used by Platform
local Baga patch applicability
kindlehf build output
Contract/BICTS evidence
```

不能只改 commit SHA 后直接宣称兼容。

---

## 3. Local Patch Policy

每个 Baga-specific patch 必须记录：

```text
patch id/name
upstream base commit
files/symbols touched
purpose
why wrapper/config alone is insufficient
expected upstream-upgrade conflict surface
tests covering patch
```

K1 direct-entry patch SHOULD 极小，只增加受控 private entry，而不重构 KOReader 主启动链。

---

## 4. FBInk

FBInk 保持其上游 native/C 实现和许可证。Baga 只在需要的 Kindle profile/backend 中通过现有或窄 binding 使用。

不得：

```text
为了统一语言复制/改写 FBInk implementation
把 FBInk API 暴露给 IKP
把 FBInk license 替换成 Baga license
```

---

## 5. Build Assets

至少保存/可重建：

```text
toolchain version
native target
build command
upstream pins
local patches
configuration flags
output file names
artifact hashes
```

K1 Gate 的关键不是“某台机器上曾经编译成功”，而是 pinned inputs 能重复得到可验证的 `kindlehf` artifact。

---

## 6. KPM / Homebrew Assets

进入 K6 后单独记录：

```text
KPM version/commit
package manifest format assumptions
install/launch/uninstall hooks
sh_integration assets
MRPI/legacy fallback envelope source/compatibility evidence
```

KPM 等 install components 不应误写进 Device Adapter dependency manifest；它们属于 Platform native build/install manifest。

---

## 7. License Gate

任何 bundling/modification/linking/invocation 方式改变时重新检查：

```text
LICENSE
NOTICE
THIRD_PARTY_NOTICES.md
docs/zh-CN/governance/02_Baga-Ink授权策略.md
```

第三方组件始终保留其上游许可义务。

---

## 8. Batch 0/2 输出

```text
[ ] dependency manifest 初版
[ ] candidate pins
[ ] local patch inventory
[ ] build toolchain inventory
[ ] license/notice mapping
[ ] artifact digest format
[ ] upgrade/re-pin procedure
```

这些资产是 K1/K2 后续可重现性的前置条件。
